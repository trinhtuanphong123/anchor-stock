# Frontend environment — only NEXT_PUBLIC_* variables are exposed to the browser.
# No secrets, DB credentials, or service-role keys here (DEC-004, DEC-011).
#
# When unset, the app falls back to mock data so the shell works without a running API.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
