# pipelines — anchor-model data and modelling steps.
#
# A library of callable functions, not a service: local CLIs and (later) Airflow tasks call the
# same entry points. Nothing here starts a scheduler or owns a process lifecycle.
