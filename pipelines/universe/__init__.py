# pipelines.universe — resolving which tickers the model runs on.
#
# The universe comes from a file, not from the database. A fresh install therefore has an
# unambiguous answer to "which tickers?" before anything has been ingested.
